---
phase: 06-progress-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/projects/sync-types.ts
  - src/projects/index-generator.ts
  - src/projects/sync-service.ts
  - src/projects/index-generator.test.ts
  - src/projects/sync-service.test.ts
autonomous: true
requirements: [PRG-01, PRG-02]
must_haves:
  truths:
    - "Workflow index JSON files are generated at .index/workflows/WF-NNN.json with computed progress counts"
    - "A workflows.json summary file is generated at .index/workflows.json"
    - "SyncService detects workflow file changes and regenerates workflow indexes incrementally"
    - "Full reindex on startup includes workflow indexes"
    - "Board index includes workflow field on each task entry"
  artifacts:
    - path: "src/projects/sync-types.ts"
      provides: "WorkflowProgressCounts, WorkflowIndex, WorkflowSummary types, workflow:changed SyncEvent, workflow field on BoardTaskEntry"
      contains: "WorkflowProgressCounts"
    - path: "src/projects/index-generator.ts"
      provides: "generateWorkflowIndex, generateWorkflowSummary functions"
      exports: ["generateWorkflowIndex", "generateWorkflowSummary"]
    - path: "src/projects/sync-service.ts"
      provides: "workflows/ directory watcher and incremental workflow index regeneration"
      contains: "workflows/"
  key_links:
    - from: "src/projects/sync-service.ts"
      to: "src/projects/index-generator.ts"
      via: "generateWorkflowIndex call in processUpdate"
      pattern: "generateWorkflowIndex"
    - from: "src/projects/index-generator.ts"
      to: "src/projects/sync-types.ts"
      via: "WorkflowIndex type import"
      pattern: "WorkflowIndex"
---

<objective>
Add workflow index generation infrastructure: types, index generators, SyncService watcher, and board task workflow field.

Purpose: Provides the data layer that all downstream consumers (status command, RPC, board UI) read from. Workflow progress becomes computable and persistable as JSON index files.
Output: WorkflowIndex/WorkflowSummary types, generateWorkflowIndex/generateWorkflowSummary functions, SyncService workflows/ watcher, BoardTaskEntry.workflow field, generateAllIndexes extended with workflow step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-progress-observability/06-CONTEXT.md
@.planning/phases/06-progress-observability/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/projects/sync-types.ts:
```typescript
export type SyncEvent =
  | { type: "project:changed"; project: string }
  | { type: "task:changed"; project: string; taskId: string }
  | { type: "task:deleted"; project: string; taskId: string }
  | { type: "queue:changed"; project: string }
  | { type: "reindex:complete"; project: string };

export interface BoardTaskEntry {
  id: string;
  title: string;
  status: string;
  priority: string;
  claimed_by: string | null;
}
```

From src/projects/types.ts:
```typescript
export type WorkflowFrontmatter = z.infer<typeof WorkflowFrontmatterSchema>;
```

From src/projects/frontmatter.ts:
```typescript
export function parseWorkflowFrontmatter(content: string, filePath: string): ParseResult<WorkflowFrontmatter>;
```

From src/hooks/bundled/workflow-status-hook.ts:
```typescript
// readWorkflowTaskStatuses is a private function -- pattern to follow, not to import.
// It reads workflow file, parses frontmatter, then reads each linked task file for status.
```

From src/projects/index-generator.ts:
```typescript
export function generateProjectIndex(frontmatter: ProjectFrontmatter): ProjectIndex;
export function generateTaskIndex(frontmatter: TaskFrontmatter): TaskIndex;
export function generateBoardIndex(tasks: TaskFrontmatter[], columns: string[]): BoardIndex;
export function generateQueueIndex(parsed: ParsedQueue): QueueIndex;
export async function writeIndexFile(filePath: string, data: unknown): Promise<void>;
export async function generateAllIndexes(projectDir: string): Promise<SyncEvent[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add workflow types and generateWorkflowIndex/generateWorkflowSummary</name>
  <files>src/projects/sync-types.ts, src/projects/index-generator.ts, src/projects/index-generator.test.ts</files>
  <read_first>
    - src/projects/sync-types.ts
    - src/projects/index-generator.ts
    - src/projects/index-generator.test.ts
    - src/projects/types.ts
    - src/hooks/bundled/workflow-status-hook.ts
    - src/projects/frontmatter.ts
  </read_first>
  <behavior>
    - Test: generateWorkflowIndex with 5 tasks (2 done, 1 in-progress, 1 review, 1 available) returns WorkflowIndex with progress.done=2, progress.claimed=1, progress.review=1, progress.available=1, progress.total=5, progress.blocked=0
    - Test: generateWorkflowIndex with all tasks done returns progress.done equal to progress.total
    - Test: generateWorkflowIndex with empty tasks list returns all zeros and total=0
    - Test: generateWorkflowIndex includes frontmatter fields (id, title, status, goal, tasks, template) and indexedAt timestamp
    - Test: generateWorkflowSummary produces array of workflow summaries with id, title, status, progress, and indexedAt
    - Test: generateBoardIndex populates workflow field from TaskFrontmatter.workflow
    - Test: generateBoardIndex sets workflow to null when task has no workflow
    - Test: generateAllIndexes reads workflows/ directory and writes .index/workflows/WF-NNN.json and .index/workflows.json
  </behavior>
  <action>
    1. In `src/projects/sync-types.ts`:
       - Add `workflow:changed` variant to SyncEvent union: `| { type: "workflow:changed"; project: string; workflowId: string }`
       - Add `workflow?: string | null` field to `BoardTaskEntry` interface
       - Add `WorkflowProgressCounts` interface with fields: `total: number`, `done: number`, `claimed: number`, `review: number`, `blocked: number`, `available: number`
       - Add `WorkflowIndex` interface: `{ id: string; title: string; status: string; goal: string; tasks: string[]; template: string | null; progress: WorkflowProgressCounts; indexedAt: string; }`
       - Add `WorkflowSummary` interface: `{ workflows: Array<{ id: string; title: string; status: string; progress: WorkflowProgressCounts; }>; indexedAt: string; }`

    2. In `src/projects/index-generator.ts`:
       - Import `WorkflowIndex`, `WorkflowSummary`, `WorkflowProgressCounts` from `./sync-types.js`
       - Import `parseWorkflowFrontmatter` from `./frontmatter.js`
       - Import `WorkflowFrontmatter` type from `./types.js`
       - Add `generateWorkflowIndex(frontmatter: WorkflowFrontmatter, taskStatuses: Map<string, string>): WorkflowIndex` function:
         - Initialize counters: done=0, claimed=0, review=0, blocked=0, available=0
         - For each entry in taskStatuses: switch on status -- "done" increments done, "in-progress" increments claimed, "review" increments review, "blocked" increments blocked, default increments available
         - Return object with frontmatter.id, frontmatter.title, frontmatter.status, frontmatter.goal, frontmatter.tasks, `frontmatter.template ?? null`, progress object with total=frontmatter.tasks.length, and indexedAt ISO string
       - Add `generateWorkflowSummary(workflows: WorkflowIndex[]): WorkflowSummary` function:
         - Map each WorkflowIndex to `{ id, title, status, progress }`, wrap in `{ workflows: [...], indexedAt: new Date().toISOString() }`
       - Update `generateBoardIndex` to populate `workflow: task.workflow ?? null` in the BoardTaskEntry construction (the `entry` object at ~line 50)
       - Update `generateAllIndexes` to add step 5 after queue index generation:
         - Read `workflows/` directory with `fs.readdir(path.join(projectDir, "workflows"))`, filter for `/^WF-\d+\.md$/`
         - For each workflow file: read content, `parseWorkflowFrontmatter`, read linked task files to build `taskStatuses` Map (same pattern as workflow-status-hook.ts), call `generateWorkflowIndex`, write to `.index/workflows/WF-NNN.json` via `writeIndexFile`
         - After all individual workflow indexes, build WorkflowSummary from collected WorkflowIndex objects, write to `.index/workflows.json`
         - Ensure `.index/workflows/` directory is created via `fs.mkdir(workflowsIndexDir, { recursive: true })` before writing
         - Wrap in try/catch (workflows/ missing = skip, consistent with tasks/ handling)

    3. In `src/projects/index-generator.test.ts`:
       - Add test suite "generateWorkflowIndex" with tests per behavior block
       - Add tests for generateBoardIndex workflow field
       - Add tests for generateAllIndexes workflow step (use tmp dir fixtures with workflow and task markdown files)
  </action>
  <verify>
    <automated>pnpm test -- src/projects/index-generator.test.ts -t "workflow"</automated>
  </verify>
  <acceptance_criteria>
    - src/projects/sync-types.ts contains `type: "workflow:changed"`
    - src/projects/sync-types.ts contains `workflow?: string | null` in BoardTaskEntry
    - src/projects/sync-types.ts contains `export interface WorkflowProgressCounts`
    - src/projects/sync-types.ts contains `export interface WorkflowIndex`
    - src/projects/sync-types.ts contains `export interface WorkflowSummary`
    - src/projects/index-generator.ts contains `export function generateWorkflowIndex`
    - src/projects/index-generator.ts contains `export function generateWorkflowSummary`
    - src/projects/index-generator.ts contains `workflow: task.workflow` in generateBoardIndex
    - src/projects/index-generator.ts contains `workflows/` read in generateAllIndexes
    - src/projects/index-generator.test.ts contains describe block with "workflow" (case-insensitive)
    - `pnpm test -- src/projects/index-generator.test.ts` exits 0
  </acceptance_criteria>
  <done>generateWorkflowIndex computes correct progress counts from task statuses, generateWorkflowSummary produces summary array, generateBoardIndex includes workflow field, generateAllIndexes writes workflow index files, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire SyncService to watch workflows/ and regenerate workflow indexes incrementally</name>
  <files>src/projects/sync-service.ts, src/projects/sync-service.test.ts</files>
  <read_first>
    - src/projects/sync-service.ts
    - src/projects/sync-service.test.ts
    - src/projects/index-generator.ts
    - src/projects/sync-types.ts
    - src/projects/frontmatter.ts
  </read_first>
  <behavior>
    - Test: processUpdate with a workflows/WF-001.md change triggers workflow index regeneration (writes .index/workflows/WF-001.json and .index/workflows.json)
    - Test: processUpdate with a workflows/WF-001.md change emits workflow:changed SyncEvent with correct project and workflowId
    - Test: processUpdate ignores non-workflow files in workflows/ directory (e.g. README.md)
    - Test: handleFileDelete for workflows/WF-001.md removes .index/workflows/WF-001.json and regenerates workflows.json summary
  </behavior>
  <action>
    1. In `src/projects/sync-service.ts`:
       - Import `generateWorkflowIndex`, `generateWorkflowSummary` from `./index-generator.js`
       - Import `parseWorkflowFrontmatter` from `./frontmatter.js`
       - Import `parseTaskFrontmatter` from `./frontmatter.js` (already imported)
       - Import `WorkflowIndex` type from `./sync-types.js`

    2. Add `else if` branch in `processUpdate()` after the `tasks/` branch (~line 220):
       ```
       } else if (relative.startsWith("workflows/") && /^WF-\d+\.md$/.test(basename)) {
       ```
       For `action === "delete"`:
         - Extract workflowId from basename: `basename.replace(".md", "")`
         - Delete `.index/workflows/${workflowId}.json` via `fs.unlink(...).catch(() => {})`
         - Regenerate workflows.json summary by calling `this.regenerateWorkflowSummary(projectDir)`
         - Emit SyncEvent: `{ type: "workflow:changed", project: projectName, workflowId }`

       For `action === "change"`:
         - Read workflow file content
         - Parse with `parseWorkflowFrontmatter(content, basename)`
         - If parse succeeds, read each linked task file to build `taskStatuses: Map<string, string>` (same pattern as the readWorkflowTaskStatuses helper in workflow-status-hook.ts -- iterate wfData.tasks, read each task file, parse frontmatter, map taskId to status)
         - Call `generateWorkflowIndex(wfData, taskStatuses)`
         - Write to `.index/workflows/${wfData.id}.json` via `writeIndexFile`
         - Regenerate workflows.json summary by calling `this.regenerateWorkflowSummary(projectDir)`
         - Emit SyncEvent: `{ type: "workflow:changed", project: projectName, workflowId: wfData.id }`

    3. Add private `regenerateWorkflowSummary(projectDir: string): Promise<void>` method:
       - Read all `.json` files from `.index/workflows/` directory (filter for `WF-*.json`, skip `workflows.json` itself if present in same dir -- actually workflows.json is at `.index/workflows.json` which is the parent, so this is fine)
       - Wait, correction: individual workflow indexes are at `.index/workflows/WF-NNN.json`, summary is at `.index/workflows.json`. So:
         - `const workflowsIndexDir = path.join(projectDir, ".index", "workflows")`
         - Read all files matching `WF-*.json` from workflowsIndexDir
         - Parse each as WorkflowIndex
         - Call `generateWorkflowSummary(allWorkflows)`
         - Write to `path.join(projectDir, ".index", "workflows.json")` via writeIndexFile

    4. In `src/projects/sync-service.test.ts`:
       - Add test cases per behavior block using tmp directory fixtures
       - Create workflow file, task files, trigger processUpdate, verify index file contents and emitted events
  </action>
  <verify>
    <automated>pnpm test -- src/projects/sync-service.test.ts -t "workflow"</automated>
  </verify>
  <acceptance_criteria>
    - src/projects/sync-service.ts contains `relative.startsWith("workflows/")`
    - src/projects/sync-service.ts contains `generateWorkflowIndex`
    - src/projects/sync-service.ts contains `workflow:changed`
    - src/projects/sync-service.ts contains `regenerateWorkflowSummary`
    - src/projects/sync-service.test.ts contains test with "workflow" (case-insensitive)
    - `pnpm test -- src/projects/sync-service.test.ts` exits 0
  </acceptance_criteria>
  <done>SyncService handles workflows/ file changes by regenerating individual and summary workflow indexes, emits workflow:changed events, handles workflow file deletion, all tests pass</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/projects/index-generator.test.ts src/projects/sync-service.test.ts` passes
- `pnpm tsgo` shows no type errors in modified files
- `pnpm check` passes (lint/format)
</verification>

<success_criteria>
- WorkflowProgressCounts, WorkflowIndex, WorkflowSummary types exported from sync-types.ts
- workflow:changed variant in SyncEvent union
- workflow field on BoardTaskEntry
- generateWorkflowIndex and generateWorkflowSummary functions exported from index-generator.ts
- generateAllIndexes includes workflow index step
- SyncService watches workflows/ directory and regenerates indexes incrementally
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-progress-observability/06-01-SUMMARY.md`
</output>
