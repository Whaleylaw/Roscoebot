---
phase: 01-schema-workflow-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/projects/schemas.ts
  - src/projects/types.ts
  - src/projects/frontmatter.ts
  - src/projects/schemas.test.ts
  - src/projects/frontmatter.test.ts
autonomous: true
requirements: [WF-01, WF-02, WF-03, DEC-04]

must_haves:
  truths:
    - "WorkflowFrontmatterSchema validates valid workflow frontmatter and rejects invalid"
    - "Workflow status field accepts exactly draft, active, paused, completed, failed"
    - "All WF-03 fields present: id, title, status, goal, goal_check, tasks, template, created, updated"
    - "TaskFrontmatterSchema accepts new optional fields with defaults and existing minimal tasks still parse"
    - "parseWorkflowFrontmatter extracts YAML and validates against WorkflowFrontmatterSchema"
  artifacts:
    - path: "src/projects/schemas.ts"
      provides: "WORKFLOW_ID_PATTERN, WorkflowFrontmatterSchema, extended TaskFrontmatterSchema, extended ProjectFrontmatterSchema"
      contains: "WorkflowFrontmatterSchema"
    - path: "src/projects/types.ts"
      provides: "WorkflowFrontmatter type export"
      contains: "WorkflowFrontmatter"
    - path: "src/projects/frontmatter.ts"
      provides: "parseWorkflowFrontmatter function"
      contains: "parseWorkflowFrontmatter"
  key_links:
    - from: "src/projects/types.ts"
      to: "src/projects/schemas.ts"
      via: "z.infer<typeof WorkflowFrontmatterSchema>"
      pattern: "WorkflowFrontmatterSchema"
    - from: "src/projects/frontmatter.ts"
      to: "src/projects/schemas.ts"
      via: "import WorkflowFrontmatterSchema"
      pattern: "WorkflowFrontmatterSchema"
---

<objective>
Define Zod schemas for workflow frontmatter, extend task and project schemas with orchestration fields, infer types, and add a workflow frontmatter parser.

Purpose: Establishes the data contracts that all downstream plans (templates, scaffold, capability registry) and future phases (orchestration, verification) depend on.
Output: WorkflowFrontmatterSchema, extended TaskFrontmatterSchema, extended ProjectFrontmatterSchema, WorkflowFrontmatter type, parseWorkflowFrontmatter function, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-workflow-foundation/01-CONTEXT.md
@.planning/phases/01-schema-workflow-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing contracts the executor needs. -->

From src/projects/schemas.ts:

```typescript
export const TASK_ID_PATTERN = /^TASK-\d+$/;

export const ProjectFrontmatterSchema = z.object({
  name: z.string(),
  status: z.enum(["active", "paused", "complete"]).default("active"),
  description: z.string().optional(),
  owner: z.string().optional(),
  tags: z.array(z.string()).default([]),
  columns: z.array(z.string()).default(["Backlog", "In Progress", "Review", "Done"]),
  dashboard: z.object({ widgets: z.array(z.string()).default([...]) }).default({...}),
  created: z.string().optional(),
  updated: z.string().optional(),
});

export const TaskFrontmatterSchema = z.object({
  id: z.string().regex(TASK_ID_PATTERN),
  title: z.string(),
  status: z.enum(["backlog", "in-progress", "review", "done", "blocked"]).default("backlog"),
  column: z.string().default("Backlog"),
  priority: z.enum(["low", "medium", "high", "critical"]).default("medium"),
  capabilities: z.array(z.string()).default([]),
  depends_on: z.array(z.string().regex(TASK_ID_PATTERN)).default([]),
  claimed_by: z.string().nullable().default(null),
  claimed_at: z.string().nullable().default(null),
  created: z.string().optional(),
  updated: z.string().optional(),
  parent: z.string().nullable().default(null),
});

export const QueueFrontmatterSchema = z.object({
  updated: z.string().optional(),
});
```

From src/projects/types.ts:

```typescript
export type ProjectFrontmatter = z.infer<typeof ProjectFrontmatterSchema>;
export type TaskFrontmatter = z.infer<typeof TaskFrontmatterSchema>;
export type QueueFrontmatter = z.infer<typeof QueueFrontmatterSchema>;
export type ParseError = {
  filePath: string;
  message: string;
  issues: Array<{ path: string; message: string; line?: number }>;
};
export type ParseResult<T> = { success: true; data: T } | { success: false; error: ParseError };
```

From src/projects/frontmatter.ts:

```typescript
// Private: extractYamlBlock(content: string): string | undefined
// Private: parseAndValidate<T>(content, filePath, schema): ParseResult<T>
export function parseProjectFrontmatter(
  content: string,
  filePath: string,
): ParseResult<ProjectFrontmatter>;
export function parseTaskFrontmatter(
  content: string,
  filePath: string,
): ParseResult<TaskFrontmatter>;
export function parseQueueFrontmatter(
  content: string,
  filePath: string,
): ParseResult<QueueFrontmatter>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WorkflowFrontmatterSchema, extend TaskFrontmatterSchema and ProjectFrontmatterSchema, add types</name>
  <files>src/projects/schemas.ts, src/projects/types.ts, src/projects/schemas.test.ts</files>
  <read_first>
    - src/projects/schemas.ts (current schemas -- see exact field list above)
    - src/projects/types.ts (current type exports)
    - src/projects/schemas.test.ts (existing test patterns for safeParse, defaults, enum validation)
  </read_first>
  <behavior>
    - WorkflowFrontmatterSchema.safeParse({ id: "WF-001", title: "Test", goal: "Do thing" }) returns success: true with status defaulting to "draft"
    - WorkflowFrontmatterSchema.safeParse({}) returns success: false (id, title, goal are required)
    - WorkflowFrontmatterSchema accepts all 5 status values: "draft", "active", "paused", "completed", "failed"
    - WorkflowFrontmatterSchema rejects status: "invalid"
    - WorkflowFrontmatterSchema defaults: tasks=[], template=null, goal_check=null
    - WorkflowFrontmatterSchema validates tasks array entries against TASK_ID_PATTERN (rejects ["bad-id"])
    - WorkflowFrontmatterSchema accepts id "WF-1000" (regex /^WF-\d+$/ not limited to 3 digits)
    - WORKFLOW_ID_PATTERN matches "WF-001", "WF-1000", rejects "WF-", "TASK-001", "wf-001"
    - TaskFrontmatterSchema.safeParse({ id: "TASK-001", title: "X" }) still returns success: true (backward compat)
    - TaskFrontmatterSchema new field defaults: workflow=null, verification_type="automatic", side_effect_class="none", approval_required=false, estimated_size="medium", execution_mode="auto"
    - TaskFrontmatterSchema accepts workflow: "WF-001" (validated against WORKFLOW_ID_PATTERN)
    - TaskFrontmatterSchema rejects workflow: "bad" (invalid pattern)
    - TaskFrontmatterSchema accepts all verification_type values: "automatic", "human", "external", "mixed"
    - TaskFrontmatterSchema accepts all side_effect_class values: "none", "reversible", "irreversible"
    - TaskFrontmatterSchema accepts all estimated_size values: "small", "medium", "large"
    - TaskFrontmatterSchema accepts all execution_mode values: "auto", "manual", "interactive"
    - ProjectFrontmatterSchema.safeParse({ name: "test" }) still returns success: true (backward compat)
    - ProjectFrontmatterSchema defaults: allowed_capabilities=[]
    - ProjectFrontmatterSchema accepts allowed_capabilities: ["code", "research"]
  </behavior>
  <action>
    **In src/projects/schemas.ts:**

    1. Add `WORKFLOW_ID_PATTERN` constant after the existing `TASK_ID_PATTERN`:
       ```typescript
       /** Pattern for workflow IDs: WF-001, WF-042, WF-1000, etc. */
       export const WORKFLOW_ID_PATTERN = /^WF-\d+$/;
       ```

    2. Add `allowed_capabilities` field to `ProjectFrontmatterSchema` (after the existing `updated` field):
       ```typescript
       allowed_capabilities: z.array(z.string()).default([]),
       ```
       Per D-14: project-level capability registry. Empty default for backward compat per pitfall 3.

    3. Add 6 new optional fields to `TaskFrontmatterSchema` (after the existing `parent` field), per D-10 defaults:
       ```typescript
       workflow: z.string().regex(WORKFLOW_ID_PATTERN).nullable().default(null),
       verification_type: z.enum(["automatic", "human", "external", "mixed"]).default("automatic"),
       side_effect_class: z.enum(["none", "reversible", "irreversible"]).default("none"),
       approval_required: z.boolean().default(false),
       estimated_size: z.enum(["small", "medium", "large"]).default("medium"),
       execution_mode: z.enum(["auto", "manual", "interactive"]).default("auto"),
       ```
       Per D-11: workflow stores workflow ID only. Per D-12: t-shirt sizes. Per D-13: three execution modes.

    4. Add `WorkflowFrontmatterSchema` after `TaskFrontmatterSchema`:
       ```typescript
       export const WorkflowFrontmatterSchema = z.object({
         id: z.string().regex(WORKFLOW_ID_PATTERN),
         title: z.string(),
         status: z.enum(["draft", "active", "paused", "completed", "failed"]).default("draft"),
         goal: z.string(),
         goal_check: z.string().nullable().default(null),
         tasks: z.array(z.string().regex(TASK_ID_PATTERN)).default([]),
         template: z.string().nullable().default(null),
         created: z.string().optional(),
         updated: z.string().optional(),
       });
       ```
       Per D-01: WF-NNN format. Per D-02: YAML frontmatter. Per D-03: explicit task list. Per D-04: template name only. Per D-05/D-06: lifecycle states. Per D-08: goal required, goal_check optional.

    **In src/projects/types.ts:**

    5. Add import for `WorkflowFrontmatterSchema` from `./schemas.js`
    6. Add: `export type WorkflowFrontmatter = z.infer<typeof WorkflowFrontmatterSchema>;`

    **In src/projects/schemas.test.ts:**

    7. Add `WORKFLOW_ID_PATTERN, WorkflowFrontmatterSchema` to the import from `./schemas.js`
    8. Add a `describe("WORKFLOW_ID_PATTERN")` block testing matches/rejections
    9. Add a `describe("WorkflowFrontmatterSchema")` block covering all behavior items above
    10. Add tests to the existing `describe("TaskFrontmatterSchema")` block for backward compat and new field defaults
    11. Add tests to the existing `describe("ProjectFrontmatterSchema")` block for `allowed_capabilities` default and explicit values

  </action>
  <verify>
    <automated>pnpm test -- src/projects/schemas.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/projects/schemas.ts contains `export const WORKFLOW_ID_PATTERN = /^WF-\d+$/`
    - src/projects/schemas.ts contains `export const WorkflowFrontmatterSchema = z.object({`
    - src/projects/schemas.ts TaskFrontmatterSchema contains `workflow: z.string().regex(WORKFLOW_ID_PATTERN).nullable().default(null)`
    - src/projects/schemas.ts TaskFrontmatterSchema contains `verification_type: z.enum(["automatic", "human", "external", "mixed"]).default("automatic")`
    - src/projects/schemas.ts TaskFrontmatterSchema contains `side_effect_class: z.enum(["none", "reversible", "irreversible"]).default("none")`
    - src/projects/schemas.ts TaskFrontmatterSchema contains `approval_required: z.boolean().default(false)`
    - src/projects/schemas.ts TaskFrontmatterSchema contains `estimated_size: z.enum(["small", "medium", "large"]).default("medium")`
    - src/projects/schemas.ts TaskFrontmatterSchema contains `execution_mode: z.enum(["auto", "manual", "interactive"]).default("auto")`
    - src/projects/schemas.ts ProjectFrontmatterSchema contains `allowed_capabilities: z.array(z.string()).default([])`
    - src/projects/types.ts contains `export type WorkflowFrontmatter = z.infer<typeof WorkflowFrontmatterSchema>`
    - src/projects/schemas.test.ts contains `describe("WorkflowFrontmatterSchema"` with at least 8 test cases
    - src/projects/schemas.test.ts contains test verifying TaskFrontmatterSchema.safeParse({ id: "TASK-001", title: "X" }) still succeeds
    - `pnpm test -- src/projects/schemas.test.ts -x` exits 0
  </acceptance_criteria>
  <done>WorkflowFrontmatterSchema validates all WF-03 fields. TaskFrontmatterSchema accepts DEC-04 fields with backward-compatible defaults. ProjectFrontmatterSchema has allowed_capabilities. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parseWorkflowFrontmatter and tests</name>
  <files>src/projects/frontmatter.ts, src/projects/frontmatter.test.ts</files>
  <read_first>
    - src/projects/frontmatter.ts (see parseAndValidate pattern, existing parser functions)
    - src/projects/frontmatter.test.ts (existing test patterns for parser tests)
    - src/projects/schemas.ts (after Task 1 -- to confirm WorkflowFrontmatterSchema export)
  </read_first>
  <behavior>
    - parseWorkflowFrontmatter with valid YAML frontmatter returns { success: true, data: WorkflowFrontmatter }
    - parseWorkflowFrontmatter with missing frontmatter returns { success: false, error.message: "No frontmatter block found" }
    - parseWorkflowFrontmatter with invalid YAML returns { success: false } with YAML parse error
    - parseWorkflowFrontmatter with missing required fields (no id) returns { success: false } with schema validation error
    - parseWorkflowFrontmatter fills defaults: status="draft", tasks=[], template=null, goal_check=null
    - parseTaskFrontmatter still works with extended schema (existing tests remain green)
  </behavior>
  <action>
    **In src/projects/frontmatter.ts:**

    1. Add `WorkflowFrontmatterSchema` to the import from `./schemas.js`:
       ```typescript
       import {
         ProjectFrontmatterSchema,
         QueueFrontmatterSchema,
         TaskFrontmatterSchema,
         WorkflowFrontmatterSchema,
       } from "./schemas.js";
       ```

    2. Add `WorkflowFrontmatter` to the type import from `./types.js`:
       ```typescript
       import type {
         ParseResult,
         ProjectFrontmatter,
         QueueFrontmatter,
         TaskFrontmatter,
         WorkflowFrontmatter,
       } from "./types.js";
       ```

    3. Add `parseWorkflowFrontmatter` function after `parseQueueFrontmatter`:
       ```typescript
       export function parseWorkflowFrontmatter(
         content: string,
         filePath: string,
       ): ParseResult<WorkflowFrontmatter> {
         return parseAndValidate(content, filePath, WorkflowFrontmatterSchema);
       }
       ```

    **In src/projects/frontmatter.test.ts:**

    4. Add a `describe("parseWorkflowFrontmatter")` block with tests for:
       - Valid workflow frontmatter parses successfully with all fields
       - Default values applied (status: "draft", tasks: [], template: null, goal_check: null)
       - Missing frontmatter block returns error
       - Invalid YAML returns error
       - Missing required field (no id) returns schema validation error
       - Valid workflow with tasks array containing TASK-NNN IDs
       - Existing parseTaskFrontmatter tests remain passing (no new tests needed, just confirm no breakage)

  </action>
  <verify>
    <automated>pnpm test -- src/projects/frontmatter.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/projects/frontmatter.ts contains `export function parseWorkflowFrontmatter(`
    - src/projects/frontmatter.ts imports `WorkflowFrontmatterSchema` from `./schemas.js`
    - src/projects/frontmatter.ts imports `WorkflowFrontmatter` type from `./types.js`
    - src/projects/frontmatter.test.ts contains `describe("parseWorkflowFrontmatter"`
    - src/projects/frontmatter.test.ts has at least 5 test cases in that describe block
    - `pnpm test -- src/projects/frontmatter.test.ts -x` exits 0
  </acceptance_criteria>
  <done>parseWorkflowFrontmatter extracts YAML frontmatter and validates against WorkflowFrontmatterSchema. All parser tests pass including existing ones.</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/projects/schemas.test.ts src/projects/frontmatter.test.ts -x` -- all schema and parser tests pass
- `pnpm tsgo` -- no type errors introduced
- Backward compat: existing minimal task `{ id: "TASK-001", title: "X" }` still parses
- Backward compat: existing minimal project `{ name: "test" }` still parses
</verification>

<success_criteria>

- WorkflowFrontmatterSchema exists and validates all WF-03 fields (id, title, status, goal, goal_check, tasks, template, created, updated)
- Workflow lifecycle states (WF-02): draft, active, paused, completed, failed all accepted
- Task orchestration fields (DEC-04): workflow, verification_type, side_effect_class, approval_required, estimated_size, execution_mode all accepted with correct defaults
- ProjectFrontmatterSchema has allowed_capabilities field (foundation for CAP-01)
- parseWorkflowFrontmatter works end-to-end (YAML extraction + Zod validation)
- All existing tests remain green (no backward compat breaks)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-schema-workflow-foundation/01-01-SUMMARY.md`
</output>
